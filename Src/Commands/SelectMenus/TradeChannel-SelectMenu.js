module.exports = {
    name: "trade",
    run: async (client, interaction, container) => {
        await interaction.reply({
            content: "Why can't I view Trade Channels? - WIP",
            ephemeral: true
        })
    }
}